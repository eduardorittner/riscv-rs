[package]
name = "rust-whisper"
version = "0.1.0"
edition = "2021"
authors = ["RISC-V ALE Team"]
description = "High-performance Rust WASM drop-in replacement for whisper.js"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
web-sys = { version = "0.3", features = ["console"] }
goblin = { version = "0.8", default-features = false, features = ["elf32", "elf64", "endian_fd"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

[package.metadata.wasm-pack.profile.release]
wasm-opt = false

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
proptest = "1.4"

[[bench]]
name = "memory_benchmarks"
harness = false


